[alias]
	mend = commit --amend
	hard = reset --hard
	puff = put --force
	delete-merged-branches = !git branch --merged master | grep -v -e 'master' -e '\\*' | xargs -n 1 git wipe-branch
	wipe-branch = !git branch -d $1 && git push origin --delete
[github]
	user = jonallured
[user]
	name = Jon Allured
	email = jon.allured@gmail.com
[core]
	excludesfile = ~/.gitignore_global
[filter "trimWhitespace"]
	clean = git-stripspace
